export const PASSPORT_STRATEGIES = {
    LOCAL: 'local',
    JWT: 'jwt'
}

export enum Role {
    PUBLIC = 'PUBLIC',
    USER = 'USER',
    ADMIN = 'ADMIN',
    ADD_USER = 'ADD_USER',
    VIEW_USER='VIEW_USER'
}

export const PUBLIC_ROUTE = 'isPublic'

export enum DB {
    USER = 'UserTest'
}