export const PASSPORT_STRATEGIES = {
    LOCAL: 'local',
    JWT: 'jwt'
}

export const JWT = {
    SECRET: "ASKbgaljsg asm hajskg jkasdfhdsckasv"
}

export enum Role {
    USER = 'USER',
    ADMIN = 'ADMIN'
}

export enum DB {
    USER = 'UserTest'
}