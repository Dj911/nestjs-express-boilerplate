export const PASSPORT_STRATEGIES = {
    LOCAL: 'local',
    JWT: 'jwt'
}

export enum Role {
    USER = 'USER',
    ADMIN = 'ADMIN',
    ADD_USER = 'ADD_USER',
    VIEW_USER='VIEW_USER'
}

export enum DB {
    USER = 'UserTest'
}